<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ZoneSystemAssistant.Views.ItemDetailPage"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <StackLayout Spacing="20" Padding="15">
        <Label Text="Reading:" FontSize="Medium" />
        <Entry Text="{Binding Item.Ev}"  x:Name="EvEntry" Keyboard="Numeric" FontSize="Large" ReturnType="Next" Focused="EvEntry_OnFocused" />
        <Label Text="Description:" FontSize="Medium" />
        <Entry Text="{Binding Item.Description}"  x:Name="DescEntry" d:Text="Item description" FontSize="Large" ReturnType="Done" Focused="DescEntry_OnFocused" />
    </StackLayout>
    
</ContentPage>